{
  "name": "agents-marketplace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @marketplace/backend && npm run build --workspace @marketplace/frontend",
    "dev": "echo 'Run npm run dev --workspace @marketplace/backend and npm run dev --workspace @marketplace/frontend in separate shells'"
  }
}
